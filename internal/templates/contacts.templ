package templates

import (
	"fmt"
	"strconv"

	"github.com/DTineli/ez/internal/forms"
	"github.com/DTineli/ez/internal/templates/components"
	"github.com/DTineli/ez/internal/store"
)

templ ContactFilter() {
	<div class="card bg-base-100 shadow-sm border border-base-300">
		<div class="card-body py-4">
			<form
				id="filter-form"
				class="flex flex-wrap items-end gap-3"
				hx-get="/admin/contacts/"
				hx-target="#main-content"
				hx-swap="innerHTML"
				hx-push-url="true"
			>
				<div class="flex-1 min-w-[160px]">
					<label class="label py-1">
						<span class="label-text text-sm font-medium">Nome Fantasia</span>
					</label>
					<input
						id="trade-name-input"
						class="input input-bordered input-sm w-full"
						type="text"
						name="trade_name"
						hx-preserve="true"
						placeholder="Buscar por nome fantasia..."
					/>
				</div>
				<div class="flex-1 min-w-[160px]">
					<label class="label py-1">
						<span class="label-text text-sm font-medium">Razão Social</span>
					</label>
					<input
						class="input input-bordered input-sm w-full"
						type="text"
						name="name"
						id="name-input"
						hx-preserve="true"
						placeholder="Buscar por razão social..."
					/>
				</div>
				<div class="w-40">
					<label class="label py-1">
						<span class="label-text text-sm font-medium">Documento</span>
					</label>
					<input
						class="input input-bordered input-sm w-full"
						type="text"
						name="document"
						id="document-input"
						hx-preserve="true"
						placeholder="CPF / CNPJ"
					/>
				</div>
				<div class="w-36">
					<label class="label py-1">
						<span class="label-text text-sm font-medium">Tipo</span>
					</label>
					<select name="contact_type" id="contact_type-input" class="select select-bordered select-sm w-full" hx-preserve="true">
						<option value="">Todos</option>
						<option value="customer">Cliente</option>
						<option value="supplier">Fornecedor</option>
					</select>
				</div>
				<div class="pb-0.5">
					<button type="submit" class="btn btn-primary btn-sm">
						Buscar
					</button>
				</div>
			</form>
		</div>
	</div>
}

templ ContactPagination(current, total int) {
	<div class="flex justify-center items-center gap-2 mt-6">
		<button
			class="btn btn-sm"
			if current <= 1 {
				disabled="true"
			}
			hx-get={ fmt.Sprintf("/admin/contacts?page=%d", current-1) }
			hx-target="#main-content"
			hx-include="#filter-form"
			hx-vals={ fmt.Sprintf("page: %d", current-1) }
			hx-swap="innerHTML"
		>
			« Anterior
		</button>
		for i := 1; i <= total; i++ {
			if i == 1 || i == total || (i >= current-2 && i <= current+2) {
				<button
					if i == current {
						class="btn btn-sm btn-active"
					} else {
						class="btn btn-sm"
					}
					hx-get={ fmt.Sprintf("/admin/contacts/?page=%d", i) }
					hx-target="#main-content"
					hx-include="#filter-form"
					hx-vals={ fmt.Sprintf("page: %d", i) }
					hx-push-url="true"
					hx-swap="innerHTML"
				>
					{ i }
				</button>
			} else if i == current-3 || i == current+3 {
				<span class="px-2">…</span>
			}
		}
		<button
			class="btn btn-sm"
			if current >= total {
				disabled="true"
			}
			hx-get={ fmt.Sprintf("/admin/contacts?page=%d", current+1) }
			hx-push-url="true"
			hx-target="#main-content"
			hx-include="#filter-form"
			hx-vals={ fmt.Sprintf("page: %d", current+1) }
			hx-swap="innerHTML"
		>
			Próximo »
		</button>
	</div>
}

templ ContactTableBody(contacts []store.Contact) {
	<tbody id="contact-data">
		if len(contacts) == 0 {
			<tr>
				<td colspan="5" class="text-center text-base-content/50 py-8">
					Nenhum contato encontrado.
				</td>
			</tr>
		}
		for _, c := range contacts {
			<tr class="hover">
				<td class="font-medium">{ c.TradeName }</td>
				<td class="text-base-content/70">{ c.Name }</td>
				<td>
					if c.ContactType == "customer" {
						<span class="badge badge-info badge-sm">Cliente</span>
					} else {
						<span class="badge badge-ghost badge-sm">Fornecedor</span>
					}
				</td>
				<td class="font-mono text-sm text-base-content/60">{ c.Document }</td>
				<td class="text-center">
					<a
						class="btn btn-ghost btn-xs"
						hx-push-url="true"
						hx-target="#main-content"
						href={ fmt.Sprintf("/admin/contacts/%v", c.ID) }
					>
						Editar
					</a>
				</td>
			</tr>
		}
	</tbody>
}

templ ContactPage(params store.ListResults[store.Contact]) {
	<div class="space-y-6">
		<div class="flex items-center justify-between">
			<div>
				<h1 class="text-2xl font-bold">Contatos</h1>
				<p class="text-base-content/60 text-sm mt-0.5">Gerencie clientes e fornecedores do tenant.</p>
			</div>
			<button
				hx-get="/admin/contacts/novo"
				hx-target="#main-content"
				hx-push-url="true"
				class="btn btn-primary btn-sm"
			>
				+ Novo Contato
			</button>
		</div>
		@ContactFilter()
		<div class="card bg-base-100 shadow-sm border border-base-300">
			<div class="card-body p-0">
				<div class="overflow-x-auto">
					<table class="table table-zebra">
						<thead>
							<tr>
								<th>Nome Fantasia</th>
								<th>Razão Social</th>
								<th>Tipo</th>
								<th>Documento</th>
								<th class="text-center">Ações</th>
							</tr>
						</thead>
						@ContactTableBody(params.Results.Results)
					</table>
				</div>
			</div>
		</div>
		@ContactPagination(params.Page, params.TotalPages)
	</div>
}

templ InviteLink(id, link string) {
	<div id="invite-link" x-data="{copied:false}" class="flex items-center gap-2">
		<label class="input input-bordered input-sm flex items-center gap-2">
			<input type="text" value={ link } id="link" x-ref="link" readonly placeholder="URL do convite" class="w-60 text-sm"/>
			<button
				type="button"
				class="text-xs text-base-content/50 hover:text-base-content transition-colors"
				@click="
				navigator.clipboard.writeText($refs.link.value);
				copied = true;
				setTimeout(() => copied = false, 2000)
				"
			>copiar</button>
		</label>
		<span x-show="copied" x-transition class="text-success text-xs">Copiado!</span>
		if link == "" {
			<button
				type="button"
				hx-post={ fmt.Sprintf("/admin/contacts/%v/create-link", id) }
				hx-target="#invite-link"
				hx-swap="innerHTML"
				class="btn btn-outline btn-sm"
			>
				Gerar Link
			</button>
		}
	</div>
}

templ ContactForm(form *forms.Form, isUpdate bool, priceTables []store.PriceTable) {
	{{
		var action string
		if isUpdate {
			action = fmt.Sprintf("/admin/contacts/%s", form.Get("id"))
		} else {
			action = "/admin/contacts"
		}

		contactTypes := []struct {
			Value string
			Label string
		}{
			{"customer", "Cliente"},
			{"supplier", "Fornecedor"},
		}

		documentTypes := []struct {
			Value string
			Label string
		}{
			{"p_juridica", "Pessoa Jurídica"},
			{"p_fisica", "Pessoa Física"},
		}
	}}
	<div id="form" class="space-y-6">
		<div class="flex items-start justify-between">
			<div>
				<h1 class="text-2xl font-bold">
					if isUpdate {
						Editar Contato
					} else {
						Novo Contato
					}
				</h1>
				if isUpdate {
					<div class="mt-1">
						@InviteLink(form.Get("id"), form.Get("invite_link"))
					</div>
				} else {
					<p class="text-base-content/60 text-sm mt-0.5">Preencha os dados para cadastrar um novo contato.</p>
				}
			</div>
			<button
				hx-get="/admin/contacts/"
				hx-target="#main-content"
				hx-push-url="true"
				class="btn btn-ghost btn-sm"
			>
				← Voltar
			</button>
		</div>
		<div class="card bg-base-100 shadow-sm border border-base-300">
			<div class="card-body">
				<form
					hx-post={ action }
					hx-target="#form"
					hx-swap="outerHTML"
					class="space-y-6"
				>
					<!-- DADOS GERAIS -->
					<div>
						<h2 class="text-sm font-semibold uppercase tracking-wider text-base-content/40 mb-3">Dados Gerais</h2>
						<div class="flex flex-wrap gap-4">
							<div class="basis-[45%] grow">
								@components.BasicInput(components.BasicInputParams{
									Name:          "name",
									Label:         "Razão Social / Nome",
									ErrorMessages: form.Errors.Get("name"),
									InputType:     "text",
									Value:         form.Get("name"),
									IsRequired:    true,
								})
							</div>
							<div class="basis-[25%] grow">
								@components.BasicInput(components.BasicInputParams{
									Name:          "trade_name",
									Label:         "Nome Fantasia",
									ErrorMessages: form.Errors.Get("trade_name"),
									InputType:     "text",
									Value:         form.Get("trade_name"),
								})
							</div>
							<div class="basis-[15%] grow">
								<label class="label py-1">
									<span class="label-text text-sm">Tipo</span>
								</label>
								<select name="contact_type" class="select select-bordered select-sm w-full">
									for _, u := range contactTypes {
										<option
											value={ u.Value }
											if form.Get("contact_type") == u.Value {
												selected
											}
										>
											{ u.Label }
										</option>
									}
								</select>
							</div>
							<div class="basis-[25%] grow">
								<label class="label py-1">
									<span class="label-text text-sm">Tabela de Preço <span class="text-error">*</span></span>
								</label>
								<select name="price_table_id" class="select select-bordered select-sm w-full" required>
									<option value="">Selecione uma tabela</option>
									for _, pt := range priceTables {
										<option
											value={ strconv.Itoa(int(pt.ID)) }
											if form.Get("price_table_id") == strconv.Itoa(int(pt.ID)) {
												selected
											}
										>
											{ pt.Name }
										</option>
									}
								</select>
								if len(form.Errors.Get("price_table_id")) > 0 {
									<p class="text-error text-xs mt-1">{ form.Errors.Get("price_table_id")[0] }</p>
								}
							</div>
						</div>
					</div>
					<div class="divider my-1"></div>
					<!-- DOCUMENTO -->
					<div>
						<h2 class="text-sm font-semibold uppercase tracking-wider text-base-content/40 mb-3">Documento</h2>
						<div class="flex flex-wrap gap-4">
							<div class="basis-[18%] grow">
								<label class="label py-1">
									<span class="label-text text-sm">Tipo</span>
								</label>
								<select name="document_type" class="select select-bordered select-sm w-full">
									for _, u := range documentTypes {
										<option
											value={ u.Value }
											if form.Get("document_type") == u.Value {
												selected
											}
										>
											{ u.Label }
										</option>
									}
								</select>
							</div>
							<div class="basis-[35%] grow form-control" x-data>
								<label class="label py-1">
									<span class="label-text text-sm">CPF / CNPJ <span class="text-error">*</span></span>
								</label>
								<input
									type="text"
									name="document"
									value={ form.Get("document") }
									required
									placeholder="000.000.000-00"
									x-mask:dynamic="$input.replace(/\D/g, '').length > 11 ? '99.999.999/9999-99' : '999.999.999-99'"
									if len(form.Errors.Get("document")) > 0 {
										class="input input-bordered w-full border-error"
									} else {
										class="input input-bordered w-full"
									}
								/>
								for _, e := range form.Errors.Get("document") {
									<span class="text-error text-xs mt-1">{ e }</span>
								}
							</div>
							<div class="basis-[25%] grow">
								@components.BasicInput(components.BasicInputParams{
									Name:          "ie",
									Label:         "IE",
									ErrorMessages: form.Errors.Get("ie"),
									InputType:     "text",
									Value:         form.Get("ie"),
								})
							</div>
						</div>
					</div>
					<div class="divider my-1"></div>
					<!-- CONTATO -->
					<div>
						<h2 class="text-sm font-semibold uppercase tracking-wider text-base-content/40 mb-3">Contato</h2>
						<div class="flex flex-wrap gap-4">
							<div class="basis-[40%] grow">
								@components.BasicInput(components.BasicInputParams{
									Name:          "email",
									Label:         "Email",
									InputType:     "email",
									ErrorMessages: form.Errors.Get("email"),
									Value:         form.Get("email"),
								})
							</div>
							<div class="basis-[25%] grow" x-data>
								@components.BasicInput(components.BasicInputParams{
									Name:          "phone",
									Label:         "Telefone",
									InputType:     "text",
									Value:         form.Get("phone"),
									ErrorMessages: form.Errors.Get("phone"),
									Mask:          "(99) 9 9999-9999",
									Placeholder:   "(99) 9 9999-9999",
								})
							</div>
						</div>
					</div>
					<div class="divider my-1"></div>
					<!-- ENDEREÇO -->
					<div>
						<h2 class="text-sm font-semibold uppercase tracking-wider text-base-content/40 mb-3">Endereço</h2>
						<div class="flex flex-wrap gap-4">
							<div class="basis-[15%] grow">
								@components.BasicInput(components.BasicInputParams{
									Name:          "zipcode",
									Label:         "CEP",
									ErrorMessages: form.Errors.Get("zipcode"),
									InputType:     "text",
									Value:         form.Get("zipcode"),
								})
							</div>
							<div class="basis-[40%] grow">
								@components.BasicInput(components.BasicInputParams{
									Name:      "street",
									Label:     "Rua",
									InputType: "text",
									Value:     form.Get("street"),
								})
							</div>
							<div class="basis-[8%] grow">
								@components.BasicInput(components.BasicInputParams{
									Name:      "number",
									Label:     "Nº",
									InputType: "text",
									Value:     form.Get("number"),
								})
							</div>
							<div class="basis-[25%] grow">
								@components.BasicInput(components.BasicInputParams{
									Name:      "neighborhood",
									Label:     "Bairro",
									InputType: "text",
									Value:     form.Get("neighborhood"),
								})
							</div>
							<div class="basis-[25%] grow">
								@components.BasicInput(components.BasicInputParams{
									Name:      "city",
									Label:     "Cidade",
									InputType: "text",
									Value:     form.Get("city"),
								})
							</div>
							<div class="basis-[8%] grow">
								@components.BasicInput(components.BasicInputParams{
									Name:      "uf",
									Label:     "UF",
									InputType: "text",
									Value:     form.Get("uf"),
								})
							</div>
						</div>
					</div>
					<div class="divider my-1"></div>
					<div class="flex justify-end gap-3">
						<button
							hx-get="/admin/contacts/"
							hx-target="#main-content"
							hx-push-url="true"
							class="btn btn-ghost btn-sm"
						>
							Cancelar
						</button>
						<button type="submit" class="btn btn-primary btn-sm">
							if isUpdate {
								Salvar Alterações
							} else {
								Cadastrar Contato
							}
						</button>
					</div>
				</form>
			</div>
		</div>
	</div>
}
